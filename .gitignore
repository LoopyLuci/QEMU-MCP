# Log files
logs/
*.log

# Secrets
.env
.secrets.json

# SSH known hosts
known_hosts

# ISO files (large binaries)
iso/*.iso
iso/*.img

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.so

# PyInstaller
*.spec

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# GUI credential store (encrypted secrets — never commit)
.local/share/qmcmcp/
.config/qmcmcp/
%LOCALAPPDATA%\qmcmcp\

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
test-results/
gui.log

# Credential store data files
credentials.json
.master_key
master_key
keyfile

# Secrets and tokens
token.txt
token_new.txt
fresh_token.txt
final_token.txt
final_pairing_token.txt
pairing_token.txt
pairing_token_full.txt
android_token.txt
pairing_qr.png

# TLS certificates and keys
cert.pem
key.pem

# Machine IDs and signing keys
.qmcmcp_machine_id
.vmharness_machine_id
.vmharness_signing_key

# Runtime files
.vm_gui.pid
gui/.vmharness_single_instance.lock
file

# Virtual environment
.venv/

# Built binaries
VM-Harness-Installer.exe

# Android build artifacts
android/build/
android/.gradle/
android/local.properties

# Audit database
.audit/audit.db

# CI build artifacts (binaries)
ci/artifacts/desktop/*.exe
ci/artifacts/desktop/*.zip
ci/artifacts/desktop/*.sha256
